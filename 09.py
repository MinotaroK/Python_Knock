import random

def randomly(text):
	changed = ""
	for char in word:
		if len(str(char)) >= 5
			char = random.shuffle(char)
		changed += char
	return changed

text = "I couldn't believe that I could actually understand what I was reading : the phenomenal power of the human mind ."

print randomly(text)